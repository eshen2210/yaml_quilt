{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"yaml_quilt","text":"<p>A Python library that enables inheritance, variables, and instantiation in YAML files via inline declarations.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install yaml_quilt\n</code></pre>"},{"location":"#overview","title":"Overview","text":"<p>YamlQuilt allows templating of YAML files using inline declarations and depth-first search (DFS) traversal, through three key features: Variables, File Inheritance, and Declarative Rules. The DFS approach allows declarative rules to persist to child elements, even across inherited files.</p> <p>YamlQuilt aims for yaml readability by avoiding {{ Helm-style }} templating and in-line programming logic:</p> <pre><code>import yaml\nfrom yaml_quilt import stitch, YamlQuiltException\n\ntry:\n    result = stitch(\n        file_path=\"root_config.yaml\",\n        directory=\"/path/to/config\",\n        Loader=yaml.SafeLoader,\n        variables={\"&lt;var1&gt;\": \"value1\"}\n    )\nexcept YamlQuiltException as e:\n    print(f\"Error: {e}\")\n</code></pre> <pre><code># config.yaml\n(patch): config_path/patch.yaml\n(override) base_key1: &lt;variable1&gt;\n---\n# patch.yaml\n(abstract) base_key1:\nbase_key2: value2\n---\n# yaml_quilt output\nbase_key1: value1\nbase_key2: value2\n</code></pre>"},{"location":"#logical-flow","title":"Logical Flow","text":"<ol> <li>Load YAML into Python objects (dict/list/scalars) via PyYAML</li> <li>Convert config-level declarations (like <code>(override_file)</code>) into top-level key declarations</li> <li>Run variable substitution using DFS repeatedly until no more substitutions can occur</li> <li>Run <code>(patch)</code> expansion as a pre-pass: expand all <code>(patch)</code> nodes before processing other keys. For each expansion, repeat steps 1\u20134 and apply inheritance rules via DFS.</li> <li>Strip declaration markers from output keys / variable maps</li> <li>Return Python objects (dict/list/scalars)</li> </ol>"},{"location":"#declaration-syntax","title":"Declaration Syntax","text":"<p>Declarations are inline prefixes. Spacing matters \u2014 always include a trailing space after the declaration:</p> <p>\u2705 <code>(override) key</code> \u274c <code>(override)key</code></p> <p>Multiple declarations can be combined:</p> <pre><code>(override) (sealed) (optional) key: &lt;variable1&gt;_some_string_&lt;variable2&gt;\n</code></pre>"},{"location":"exceptions/","title":"Exceptions","text":"<p>All exceptions raised by yaml_quilt use a base exception class: <code>YamlQuiltException</code>.</p> Exception Cause <code>NoOverrideException</code> Tried to override a key without <code>(override)</code> <code>AbstractKeyNotImplementedException</code> Sub YAML did not implement an <code>(abstract)</code> key <code>KeySealedException</code> Attempted to override a <code>(sealed)</code> key or variable <code>ConflictingDeclarationException</code> Incompatible declarations applied together <code>InvalidInstantiationException</code> Bad <code>(patch)</code> structure <code>CircularInheritanceException</code> Cyclic <code>(patch)</code> references <code>InvalidDeclarationException</code> Declaration with incorrect key/value content or format <code>MismatchedValueTypeException</code> Declaration used with incorrect value type (e.g., <code>(append)</code> expects a list but received a dict) <code>AmbiguousVariableException</code> Overlapping variable keys detected in the same string"},{"location":"inheritance/","title":"Inheritance","text":"<p><code>(patch)</code> loads another YAML file and inherits it into the current node while preserving depth hierarchy.</p> <pre><code>(patch): path/to/base.yaml\n</code></pre> <p>Inherit with variables:</p> <pre><code>(patch):\n  (path): path/to/base.yaml\n  (variables):\n    &lt;name&gt;: service_a\n    &lt;port&gt;: 8080\n</code></pre> <p>Inherit multiple files (applied top to bottom):</p> <pre><code>(patch):\n  - path/to/base1.yaml\n  - path/to/base2.yaml\n  - (path): path/to/base3.yaml\n    (variables):\n      &lt;name&gt;: base_name3\n</code></pre> <p>Variables are scoped locally to each YAML file unless <code>(global)</code> or <code>(carryover)</code> is used. <code>(variables)</code> in the sub YAML do not pass to base YAMLs, and <code>(variables)</code> declared within <code>(patch)</code> are scoped only to that base YAML.</p> <p>Example:</p> <pre><code># Sub YAML\n(variables):\n  &lt;name&gt;: sub_name\nsub_name: &lt;name&gt;\nsub_key: sub_value\nservices:\n  base_service1:\n    (patch):\n      (path): path/to/base.yaml\n      (variables):\n        &lt;name&gt;: service_a\n        &lt;port&gt;: 8080\n  base_service2:\n    (patch):\n      (path): path/to/base.yaml\n      (variables):\n        &lt;name&gt;: service_b\n        &lt;port&gt;: 8080\n---\n# Base YAML\nbase_name: &lt;name&gt;\nbase_port: &lt;port&gt;\n</code></pre> <p>Renders to:</p> <pre><code>sub_name: sub_name\nsub_key: sub_value\nservices:\n  base_service1:\n    base_name: service_a\n    base_port: 8080\n  base_service2:\n    base_name: service_b\n    base_port: 8080\n</code></pre>"},{"location":"inheritance/#override-abstract-sealed-private","title":"<code>(override)</code>, <code>(abstract)</code>, <code>(sealed)</code>, <code>(private)</code>","text":"<p>These keywords modify the behavior of <code>(patch)</code> inheritance.</p> <ul> <li><code>(override)</code>: When the same key exists in both sub and base, the sub value is used.</li> <li><code>(abstract)</code>: The sub YAML must implement this key at the same depth, or <code>stitch()</code> throws <code>AbstractKeyNotImplementedException</code>.</li> <li><code>(sealed)</code>: A sub key may not <code>(override)</code> this key, or <code>stitch()</code> throws <code>KeySealedException</code>.</li> <li><code>(private)</code>: The key is deleted before the <code>(patch)</code> inheritance process.</li> </ul> <p>These keywords persist to all child elements, including <code>(patch)</code> YAMLs within those child elements.</p> <pre><code># Base YAML\n(abstract) required_key:\n(sealed) locked_key: base_locked\n(private) internal_only: should_not_render\nshared_key: base_shared\n</code></pre> <pre><code># Sub YAML\n(patch): base.yaml\nrequired_key: provided_by_sub\n# (override) locked_key: sub_locked  # Would throw KeySealedException\n(override) shared_key: sub_shared\n</code></pre> <p>Renders to:</p> <pre><code>required_key: provided_by_sub\nlocked_key: base_locked\nshared_key: sub_shared\n</code></pre>"},{"location":"inheritance/#append-prepend-merge","title":"<code>(append)</code>, <code>(prepend)</code>, <code>(merge)</code>","text":"<p>These keywords modify <code>(patch)</code> inheritance when both the sub and base values are sequences.</p> <ul> <li><code>(append)</code>: Appends the sub sequence after the base sequence.</li> <li><code>(prepend)</code>: Inserts the sub sequence before the base sequence.</li> <li><code>(merge)</code>: Each index of the sub sequence applies inheritance rules against the matching index of the base sequence.</li> </ul> <p>These keywords do not persist to child elements and only apply to matching keys at the same depth. If either value is not a sequence, <code>stitch()</code> throws <code>MismatchedValueTypeException</code>.</p> <pre><code># Base YAML\nappend_key:\n  - base_key3: data3\n  - base_key4: data4\nprepend_key:\n  - base_key3: data3\n  - base_key4: data4\nmerge_key:\n  - base_key3: data3\n  - base_key4: data4\noverride_key:\n  - base_key3: data3\n  - base_key4: data4\n---\n# Sub YAML\n(patch): path/to/base.yaml\n(append) append_key:\n  - sub_key1: data1\n  - sub_key2: data2\n(prepend) prepend_key:\n  - sub_key1: data1\n  - sub_key2: data2\n(merge) merge_key:\n  - sub_key1: data1\n    (override) base_key3: overriden_data\n  - sub_key2: data2\n(override) override_key:\n  - sub_key1: data1\n  - sub_key2: data2\n</code></pre> <p>Renders to:</p> <pre><code>append_key:\n  - base_key3: data3\n  - base_key4: data4\n  - sub_key1: data1\n  - sub_key2: data2\nprepend_key:\n  - sub_key1: data1\n  - sub_key2: data2\n  - base_key3: data3\n  - base_key4: data4\nmerge_key:\n  - sub_key1: data1\n    base_key3: overriden_data\n  - sub_key2: data2\n    base_key4: data4\noverride_key:\n  - sub_key1: data1\n  - sub_key2: data2\n</code></pre>"},{"location":"inheritance/#file-wide-keywords-abstract_file-sealed_file-override_file","title":"File-wide Keywords: <code>(abstract_file)</code>, <code>(sealed_file)</code>, <code>(override_file)</code>","text":"<p>File keywords apply a keyword to every top-level key in the file.</p> <pre><code>(abstract_file)\nabstract_key1: must_be_implemented\n  abstract_subkey: must_be_implemented\nabstract_key2: must_be_implemented\n</code></pre> <p>Is equivalent to:</p> <pre><code>(abstract) abstract_key1: must_be_implemented\n  abstract_subkey: must_be_implemented  # Also abstract \u2014 abstract persists to child elements\n(abstract) abstract_key2: must_be_implemented\n</code></pre>"},{"location":"variables/","title":"Variables","text":"<p>Variables substitute scalar string values across the YAML file.</p> <p>Variables can be added as Python arguments:</p> <pre><code>injected_variables = {\n    \"&lt;name&gt;\": \"my_service\",\n    \"&lt;port&gt;\": 8080\n}\n\nresult = stitch(\n    file_path=\"root_config.yaml\",\n    directory=\"/path/to/config\",\n    Loader=yaml.SafeLoader,\n    variables=injected_variables\n)\n</code></pre> <p>And/or as <code>(patch)</code> instantiation parameters:</p> <pre><code>(patch):\n  - (path): /path/to/config/config.yaml\n  - (variables):\n      &lt;name&gt;: my_service\n      &lt;port&gt;: 8080\n</code></pre> <p>And/or as a <code>(variables)</code> definition in the root of your dict YAML:</p> <pre><code>(variables):\n  &lt;name&gt;: my_service\n  &lt;port&gt;: 8080\n</code></pre> <p>If the root of your YAML is a list, declare <code>(variables)</code> as a list item instead:</p> <pre><code>- (variables):\n    &lt;var1&gt;: my_service\n    &lt;var2&gt;: 8080\n</code></pre> <p>Then reference them anywhere in scalar (sub)string values:</p> <pre><code>service:\n  name: &lt;name&gt;\n  port: &lt;port&gt;\n</code></pre> <p>It's recommended to use distinct variable tokens (e.g., <code>&lt;variable&gt;</code> with angle brackets) to avoid accidental substring replacement. If two variable keys overlap inside the same string, <code>stitch()</code> will raise <code>AmbiguousVariableException</code>.</p>"},{"location":"variables/#replacement-behavior","title":"Replacement Behavior","text":"<p>A scalar variable can be substituted into a YAML string value:</p> <pre><code>(variables):\n  &lt;int-var&gt;: 1\n  &lt;string-var&gt;: value\n\nfull_replacement: &lt;int-var&gt;\nsubstring_replacement: prefix-&lt;string-var&gt;\nmultiple_substring_replacement: prefix-&lt;string-var&gt;-&lt;string-var&gt;\n</code></pre> <p>Renders to:</p> <pre><code>full_replacement: 1\nsubstring_replacement: prefix-value\nmultiple_substring_replacement: prefix-value-value\n</code></pre> <p>If a variable value is a dict or list, it replaces the entire YAML node, not a substring.</p> <ul> <li>\u2705 Allowed: <code>key: &lt;some_dict_or_list&gt;</code></li> <li>\u274c Not allowed: <code>key: \"prefix-&lt;some_dict_or_list&gt;\"</code></li> </ul> <p>Example:</p> <pre><code>(variables):\n  &lt;variable_dict&gt;:\n    replaced_key1: replaced_value1\n    replaced_key2:\n      - replaced_item1\n  &lt;var1&gt;: val1\n  &lt;var2&gt;: val2\n\nroot:\n  key: &lt;variable_dict&gt;\n  key2: &lt;var1&gt;-string-&lt;var2&gt;\n</code></pre> <p>Renders to:</p> <pre><code>root:\n  key:\n    replaced_key1: replaced_value1\n    replaced_key2:\n      - replaced_item1\n  key2: val1-string-val2\n</code></pre>"},{"location":"variables/#precedence","title":"Precedence","text":"<p>Variables are applied in this order (earlier sources have higher priority):</p> <ol> <li><code>variables=</code> passed to <code>stitch()</code> (only for the root YAML load)</li> <li>Instantiation variables from the <code>(variables)</code> block under the <code>(patch)</code> that loaded the current YAML</li> <li><code>(global)</code> variables</li> <li>The local <code>(variables)</code> block at the root of the current YAML</li> </ol> <p>Variable scope is limited to the YAML file it is applied to, with the exception of <code>(global)</code> and <code>(carryover)</code> variables which can persist through nested <code>(patch)</code> calls.</p>"},{"location":"variables/#global","title":"<code>(global)</code>","text":"<p><code>(global)</code> variables are visible to the local YAML scope and all nested <code>(patch)</code> YAMLs.</p> <pre><code># Sub YAML\n(variables):\n  (global) &lt;env&gt;: prod\nconfig:\n  (patch):\n    (path): path/to/baselevel1.yaml\n    (variables):\n      (global) &lt;api&gt;: v1\n---\n# Base YAML Level 1\n# path/to/baselevel1.yaml\n(patch): path/to/baselevel2.yaml\n---\n# Base YAML Level 2\n# path/to/baselevel2.yaml\napi: &lt;api&gt;\nenv: &lt;env&gt;\n</code></pre> <p>Renders to:</p> <pre><code>config:\n  api: v1\n  env: prod\n</code></pre>"},{"location":"variables/#override-abstract-sealed","title":"<code>(override)</code>, <code>(abstract)</code>, <code>(sealed)</code>","text":"<p>These keywords modify the behavior of variables defined under <code>(variables)</code>.</p> <ul> <li><code>(override)</code>: A higher-priority scope overrides the value of the same key in a lower-priority scope.</li> <li><code>(abstract)</code>: The higher-priority source must implement this key, or <code>stitch()</code> throws <code>AbstractKeyNotImplementedException</code>.</li> <li><code>(sealed)</code>: Lower-priority sources may not override this variable, or <code>stitch()</code> throws <code>KeySealedException</code>.</li> </ul> <p>These keywords persist to all child elements, including <code>(patch)</code> YAMLs within those child elements.</p> <pre><code># Base YAML\n(variables):\n  (abstract) &lt;required_var&gt;:\n  (sealed) &lt;locked_var&gt;: base_sealed\n  &lt;shared_var&gt;: base_value\n\ndict:\n  required: &lt;required_var&gt;\n  locked: &lt;locked_var&gt;\n  shared: &lt;shared_var&gt;\n---\n# Sub YAML\n(patch):\n  (path): path/to/base.yaml\n  (variables):\n    &lt;required_var&gt;: provided_by_sub\n    # (override) &lt;locked_var&gt;: sub_locked   # Would throw KeySealedException\n    (override) &lt;shared_var&gt;: sub_overriden\n</code></pre> <p>Renders to:</p> <pre><code>dict:\n  required: provided_by_sub\n  locked: base_sealed\n  shared: sub_overriden\n</code></pre>"},{"location":"variables/#optional","title":"<code>(optional)</code>","text":"<p>For <code>(optional)</code> keys, if its value is not a variable reference the key is removed. <code>(optional)</code> values must be a scalar or <code>stitch()</code> will throw <code>InvalidDeclarationException</code>.</p> <pre><code>(variables):\n  &lt;defined&gt;: ok\n\ndict:\n  (optional) keep_me: &lt;defined&gt;\n  (optional) drop_me: &lt;missing&gt;\nlist:\n  - (optional) &lt;defined&gt;\n  - (optional) &lt;missing&gt;\n</code></pre> <p>Renders to:</p> <pre><code>dict:\n  keep_me: ok\nlist:\n  - ok\n</code></pre>"},{"location":"variables/#default","title":"<code>(default)</code>","text":"<p>The value must contain the delimiter <code>|</code> (whitespace on both sides). If the left substring is a variable reference, the entire value is replaced by the variable. Otherwise, the entire value is replaced by the right substring.</p> <pre><code>(variables):\n  &lt;val1&gt;: replaced_val1\n\ndict:\n  (default) key1: &lt;val1&gt; | default_val1\n  (default) key2: &lt;val2&gt; | default_val2\nlist:\n  - (default) &lt;val1&gt; | default_val1\n  - (default) &lt;val2&gt; | default_val2\n</code></pre> <p>Renders to:</p> <pre><code>dict:\n  key1: replaced_val1\n  key2: default_val2\nlist:\n  - replaced_val1\n  - default_val2\n</code></pre> <p>The default value is parsed as a Python literal when possible (e.g., <code>2</code> becomes an int).</p>"},{"location":"variables/#enable","title":"<code>(enable)</code>","text":"<p><code>(enable)</code> keeps its parent block if its value == <code>True</code>, otherwise deletes it.</p> <ul> <li>Parent is a dict: keeps or deletes the parent key and entire dict.</li> <li>Parent is a list and <code>(enable)</code> is the only list item: keeps or deletes the entire list.</li> <li>Parent is a list and <code>(enable)</code> is a key within a dict list item: keeps or deletes only that item.</li> </ul> <pre><code>(variables):\n  &lt;enable_dict&gt;: true\n  &lt;enable_list&gt;: true\n  &lt;enable_list_item&gt;: true\n  &lt;enable_key&gt;: true\n\nroot:\n  enabled_dict:\n    (enable): &lt;enable_dict&gt;\n    key1: value1\n    key2: value2\n  disabled_dict:\n    (enable): &lt;missing_or_false&gt;\n    key3: value3\n    key4: value4\n  enabled_list:\n    - (enable): &lt;enable_list&gt;\n    - item1\n    - item2\n  disabled_list:\n    - (enable): &lt;missing_or_false&gt;\n    - itemA\n    - itemB\n  list_of_dicts:\n    - (enable): &lt;enable_list_item&gt;\n      enabled_key1: value1\n      enabled_key2: value2\n    - (enable): &lt;missing_or_false&gt;\n      disabled_keyA: valueA\n      disabled_keyB: valueB\n  keys:\n    (enable) &lt;enable_key&gt; | enabled_key1: value1\n    (enable) &lt;enable_key&gt; | enabled_key2: value2\n    (enable) &lt;missing_or_false&gt; | disabled_key1: value3\n</code></pre> <p>Renders to:</p> <pre><code>root:\n  enabled_dict:\n    key1: value1\n    key2: value2\n  enabled_list:\n    - item1\n    - item2\n  list_of_dicts:\n    - enabled_key1: value1\n      enabled_key2: value2\n  keys:\n    enabled_key1: value1\n    enabled_key2: value2\n</code></pre> <p>Note that <code>(enable)</code>, if declared at a key level, requires the delimiter <code>|</code> seprating the final key string from the declaration and variable. Any string to the left of the delimiter <code>|</code>, minutes the <code>(enable)</code> declaration is considered as the variable.</p>"},{"location":"variables/#carryover","title":"<code>(carryover)</code>","text":"<p>A <code>(carryover)</code> variable is a valueless key used inside <code>(patch)</code> variables to pull a variable from the sub YAML scope into the base YAML scope.</p> <pre><code># Sub YAML\n(variables):\n  &lt;var&gt;: sub_value\n(patch):\n  (path): path/to/base.yaml\n  (variables):\n    (carryover) &lt;var&gt;:\n---\n# Base YAML\nbase_key: &lt;var&gt;\n</code></pre> <p>Renders to:</p> <pre><code>base_key: sub_value\n</code></pre>"}]}